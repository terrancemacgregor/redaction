// HomePage.tsx
const HomePage: React.FC = () => {
    return (
        <div>
            <h1>Redaction App</h1>
    <p>The application is a redaction engine designed to allow users to upload PDF files, extract, and redact.</p>
    <button>Upload File</button> {/* Example button */}
    </div>
);
};

export default HomePage;
